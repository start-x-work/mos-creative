# Gemini API key (BYOK).
# In the Web UI this is entered in-browser and stored only in sessionStorage.
# This file exists solely for local library/example usage; the key is never sent
# to any Start-X server. The only network destination is Google's Gemini API.
GEMINI_API_KEY=
